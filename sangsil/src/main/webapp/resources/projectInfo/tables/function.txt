DELIMITER $$

DROP FUNCTION IF EXISTS FN_GET_CD_NM$$
 
CREATE FUNCTION FN_GET_CD_NM(cdId VARCHAR(10)) RETURNS VARCHAR(100)

BEGIN

DECLARE result VARCHAR(100) DEFAULT '';

SELECT cd_nm INTO result FROM t_cd where cd_id = cdId;
 
RETURN result;
END $$
DELIMITER ;